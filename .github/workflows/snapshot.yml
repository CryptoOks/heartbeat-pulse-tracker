name: snapshot

on:
  schedule:
    - cron: "*/30 * * * *"   # кожні 30 хв (UTC)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Update data (create snapshot file)
        run: python -u scripts/update.py

      - name: Commit if changed (diff-guard) as YOU
        env:
          # ЗАМІНИ на свої no-reply дані акаунта:
          GIT_AUTHOR_NAME: "CryptoOks"
          GIT_AUTHOR_EMAIL: "96268693+CryptoOks@users.noreply.github.com"
          GIT_COMMITTER_NAME: "CryptoOks"
          GIT_COMMITTER_EMAIL: "96268693+CryptoOks@users.noreply.github.com"
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          git config user.name  "$GIT_AUTHOR_NAME"
          git config user.email "$GIT_AUTHOR_EMAIL"
          git add -A
          if git diff --cached --quiet; then
            echo "[workflow] No staged changes — skip commit."
          else
            git commit -m "snapshot: $(date -u +"%Y-%m-%d %H:%M:%SZ")"
            git push "https://$GH_TOKEN@github.com/${{ github.repository }}.git" "HEAD:${{ github.ref_name }}"
            echo "[workflow] push DONE"
          fi
